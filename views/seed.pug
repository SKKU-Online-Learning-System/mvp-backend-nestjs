extends layout

block content 
	br
	h2 Data Status
	p admin: #{dataCount.admin}
	p user: #{dataCount.user}
	p category1: #{dataCount.category1}
	p category2: #{dataCount.category2}
	p course: #{dataCount.course}
	p hashtag: #{dataCount.hashtag}
	p course_hashtag: #{dataCount.course_hashtag}
	p section : #{dataCount.section} 
	p lecture: #{dataCount.lecture}
	p history: #{dataCount.history}
	p wishlist: #{dataCount.wishlist}
	p question: #{dataCount.question}
	p answer: #{dataCount.answer}
